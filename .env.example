# 이 MCP 서버가 리슨할 포트.
PORT=3000

# whiteboard-server 의 REST API 베이스 URL. list_catalog/save_as_board 도구에서만 쓰인다.
WHITEBOARD_API_ORIGIN=http://localhost:8080

# "/view/{token}" 공유 링크를 절대 URL로 만드는 데 쓰는 whiteboard-web 오리진.
WHITEBOARD_WEB_ORIGIN=http://localhost:5173
